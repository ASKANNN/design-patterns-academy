{
  "slug": "template-method",
  "name": "Template Method",
  "category": "behavioral",
  "complexity": 1,
  "popularity": 2,
  "also_known_as": [
    "Hook Method"
  ],
  "tags": [
    "behavioral",
    "skeleton",
    "inheritance"
  ],
  "intent": {
    "en": "Template Method fixes the overall shape of an algorithm in a base class while leaving individual steps for subclasses to fill in, so the sequence of operations never changes even though its details do.",
    "ru": "Шаблонный метод закрепляет общую последовательность алгоритма в базовом классе, оставляя отдельные шаги на усмотрение подклассов, - порядок операций всегда один и тот же, даже если детали каждого шага меняются."
  },
  "problem": {
    "en": "Imagine you're building a reporting tool that generates documents in different formats: HTML and Markdown. Both formats follow the same overall structure - a title header, a list of content rows, and a footer - but the exact markup for each step differs.\n\nYou could copy the entire algorithm into each report class. But then changing the overall flow (say, adding a timestamp section) requires modifying every class individually.\n\nAlternatively, you could pack all the logic into one class and use conditionals to choose the output format - but that violates the Open/Closed Principle every time a new format is added.",
    "ru": "Представьте, что вы создаёте инструмент для генерации отчётов в разных форматах: HTML и Markdown. Оба формата следуют одной и той же общей структуре - заголовок с названием, список строк с содержимым и нижний колонтитул - но конкретная разметка для каждого шага различается.\n\nМожно скопировать весь алгоритм в каждый класс отчёта. Но тогда изменение общего потока (например, добавление секции с временной меткой) потребует изменения каждого класса отдельно.\n\nКак вариант, можно поместить всю логику в один класс и использовать условные операторы для выбора формата вывода - но это нарушает принцип открытости/закрытости при добавлении каждого нового формата."
  },
  "solution": {
    "en": "The Template Method pattern suggests breaking the algorithm into steps, turning each step into a method, and putting a series of calls to those methods inside a single **template method** in the abstract class.\n\nThe steps may either be `abstract` (forcing subclasses to provide an implementation) or have a default implementation that subclasses can optionally override (**hooks**).\n\nSubclasses can override specific steps without touching the template method itself. This guarantees that the overall algorithm structure stays the same while individual steps remain customizable.",
    "ru": "Паттерн Шаблонный метод предлагает разбить алгоритм на шаги, оформить каждый шаг как метод и поместить серию вызовов этих методов в единственный **шаблонный метод** абстрактного класса.\n\nШаги могут быть `abstract` (принуждая подклассы предоставлять реализацию) или иметь реализацию по умолчанию, которую подклассы могут опционально переопределить (**хуки**).\n\nПодклассы могут переопределять конкретные шаги, не трогая шаблонный метод. Это гарантирует неизменность общей структуры алгоритма при свободной настройке отдельных шагов."
  },
  "structure": {
    "description": {
      "en": "The AbstractClass declares the template method and all algorithm steps. Some steps are abstract; others are hooks with empty default implementations. ConcreteClass overrides the steps it needs to customize.",
      "ru": "Абстрактный класс объявляет шаблонный метод и все шаги алгоритма. Одни шаги являются абстрактными, другие - хуками с пустой реализацией по умолчанию. Конкретный класс переопределяет нужные шаги."
    },
    "participants": [
      {
        "name": "AbstractClass",
        "role": {
          "en": "Declares the template method that defines the algorithm skeleton. Also declares abstract primitive operations that subclasses must implement, and optional hook methods with empty or default implementations. In the diagram: ReportGenerator.",
          "ru": "Объявляет шаблонный метод, определяющий скелет алгоритма. Также объявляет абстрактные примитивные операции, которые подклассы должны реализовать, и опциональные методы-хуки с пустой или стандартной реализацией. На диаграмме: ReportGenerator."
        }
      },
      {
        "name": "ConcreteClass",
        "role": {
          "en": "Implements the abstract primitive operations to carry out the subclass-specific steps of the algorithm. Does not override the template method itself. In the diagram: HtmlReport, MarkdownReport.",
          "ru": "Реализует абстрактные примитивные операции для выполнения специфичных для подкласса шагов алгоритма. Не переопределяет сам шаблонный метод. На диаграмме: HtmlReport, MarkdownReport."
        }
      }
    ]
  },
  "implementation": {
    "javascript": "class ReportGenerator {\n  generate(title, rows) {\n    const parts = [\n      this.header(title),\n      this.body(rows),\n      this.footer(),\n    ];\n    return parts.filter(Boolean).join('\\n');\n  }\n\n  header(title) { return ''; }\n  footer()      { return ''; }\n\n  body(rows) {\n    throw new Error('body() must be implemented');\n  }\n}\n\nclass HtmlReport extends ReportGenerator {\n  header(title) { return `<h1>${title}</h1><ul>`; }\n  body(rows)    { return rows.map(r => `  <li>${r}</li>`).join('\\n'); }\n  footer()      { return '</ul>'; }\n}\n\nclass MarkdownReport extends ReportGenerator {\n  header(title) { return `# ${title}\\n`; }\n  body(rows)    { return rows.map(r => `- ${r}`).join('\\n'); }\n}\n\nconst rows = ['Design Patterns', 'Clean Code', 'SOLID'];\n\nconst html = new HtmlReport();\nconsole.log(html.generate('Books', rows));\n\nconst md = new MarkdownReport();\nconsole.log(md.generate('Books', rows));",
    "typescript": "abstract class ReportGenerator {\n  generate(title: string, rows: string[]): string {\n    const parts = [\n      this.header(title),\n      this.body(rows),\n      this.footer(),\n    ];\n    return parts.filter(Boolean).join('\\n');\n  }\n\n  protected header(_title: string): string { return ''; }\n  protected footer(): string               { return ''; }\n\n  protected abstract body(rows: string[]): string;\n}\n\nclass HtmlReport extends ReportGenerator {\n  protected header(title: string): string {\n    return `<h1>${title}</h1><ul>`;\n  }\n  protected body(rows: string[]): string {\n    return rows.map(r => `  <li>${r}</li>`).join('\\n');\n  }\n  protected footer(): string { return '</ul>'; }\n}\n\nclass MarkdownReport extends ReportGenerator {\n  protected header(title: string): string { return `# ${title}\\n`; }\n  protected body(rows: string[]): string {\n    return rows.map(r => `- ${r}`).join('\\n');\n  }\n}\n\nconst rows = ['Design Patterns', 'Clean Code', 'SOLID'];\n\nconst html = new HtmlReport();\nconsole.log(html.generate('Books', rows));\n\nconst md = new MarkdownReport();\nconsole.log(md.generate('Books', rows));",
    "java": "abstract class ReportGenerator {\n    public final String generate(String title, String[] rows) {\n        StringBuilder sb = new StringBuilder();\n        String h = header(title);\n        if (!h.isEmpty()) sb.append(h).append('\\n');\n        sb.append(body(rows));\n        String f = footer();\n        if (!f.isEmpty()) sb.append('\\n').append(f);\n        return sb.toString();\n    }\n\n    protected String header(String title) { return \"\"; }\n    protected String footer()             { return \"\"; }\n\n    protected abstract String body(String[] rows);\n}\n\nclass HtmlReport extends ReportGenerator {\n    protected String header(String title) { return \"<h1>\" + title + \"</h1><ul>\"; }\n    protected String body(String[] rows) {\n        StringBuilder sb = new StringBuilder();\n        for (String r : rows) sb.append(\"  <li>\").append(r).append(\"</li>\\n\");\n        return sb.toString().stripTrailing();\n    }\n    protected String footer() { return \"</ul>\"; }\n}\n\nclass MarkdownReport extends ReportGenerator {\n    protected String header(String title) { return \"# \" + title + \"\\n\"; }\n    protected String body(String[] rows) {\n        StringBuilder sb = new StringBuilder();\n        for (String r : rows) sb.append(\"- \").append(r).append('\\n');\n        return sb.toString().stripTrailing();\n    }\n}\n\nString[] rows = {\"Design Patterns\", \"Clean Code\", \"SOLID\"};\n\nSystem.out.println(new HtmlReport().generate(\"Books\", rows));\n\nSystem.out.println(new MarkdownReport().generate(\"Books\", rows));",
    "csharp": "abstract class ReportGenerator\n{\n    public string Generate(string title, string[] rows)\n    {\n        var parts = new[] { Header(title), Body(rows), Footer() };\n        return string.Join(\"\\n\", parts.Where(p => !string.IsNullOrEmpty(p)));\n    }\n\n    protected virtual string Header(string title) => \"\";\n    protected virtual string Footer()             => \"\";\n\n    protected abstract string Body(string[] rows);\n}\n\nclass HtmlReport : ReportGenerator\n{\n    protected override string Header(string title) => $\"<h1>{title}</h1><ul>\";\n    protected override string Body(string[] rows)  =>\n        string.Join(\"\\n\", rows.Select(r => $\"  <li>{r}</li>\"));\n    protected override string Footer() => \"</ul>\";\n}\n\nclass MarkdownReport : ReportGenerator\n{\n    protected override string Header(string title) => $\"# {title}\\n\";\n    protected override string Body(string[] rows)  =>\n        string.Join(\"\\n\", rows.Select(r => $\"- {r}\"));\n}\n\nstring[] rows = { \"Design Patterns\", \"Clean Code\", \"SOLID\" };\n\nConsole.WriteLine(new HtmlReport().Generate(\"Books\", rows));\n\nConsole.WriteLine(new MarkdownReport().Generate(\"Books\", rows));",
    "python": "from __future__ import annotations\nfrom abc import ABC, abstractmethod\n\n\nclass ReportGenerator(ABC):\n    def generate(self, title: str, rows: list[str]) -> str:\n        parts = [self.header(title), self.body(rows), self.footer()]\n        return '\\n'.join(p for p in parts if p)\n\n    def header(self, title: str) -> str: return ''\n    def footer(self) -> str:             return ''\n\n    @abstractmethod\n    def body(self, rows: list[str]) -> str: ...\n\n\nclass HtmlReport(ReportGenerator):\n    def header(self, title: str) -> str:\n        return f'<h1>{title}</h1><ul>'\n\n    def body(self, rows: list[str]) -> str:\n        return '\\n'.join(f'  <li>{r}</li>' for r in rows)\n\n    def footer(self) -> str:\n        return '</ul>'\n\n\nclass MarkdownReport(ReportGenerator):\n    def header(self, title: str) -> str:\n        return f'# {title}\\n'\n\n    def body(self, rows: list[str]) -> str:\n        return '\\n'.join(f'- {r}' for r in rows)\n\n\nrows = ['Design Patterns', 'Clean Code', 'SOLID']\n\nhtml = HtmlReport()\nprint(html.generate('Books', rows))\n\nmd = MarkdownReport()\nprint(md.generate('Books', rows))"
  },
  "walkthrough": {
    "javascript": [
      {
        "lines": [
          1,
          9
        ],
        "title": {
          "en": "generate() is the template method - it fixes the algorithm's skeleton",
          "ru": "generate() - шаблонный метод, он фиксирует скелет алгоритма"
        },
        "explanation": {
          "en": "`generate` always calls `this.header(title)`, `this.body(rows)`, `this.footer()` in that order, filters out empty strings, and joins them - subclasses can't reorder or skip these steps, only fill in what each one produces.",
          "ru": "`generate` всегда вызывает `this.header(title)`, `this.body(rows)`, `this.footer()` именно в этом порядке, отфильтровывает пустые строки и склеивает результат - подклассы не могут изменить порядок или пропустить шаги, они могут лишь наполнить каждый шаг содержимым."
        }
      },
      {
        "lines": [
          11,
          16
        ],
        "title": {
          "en": "header/footer are optional hooks, body is a required hook",
          "ru": "header/footer - необязательные хуки, а body - обязательный"
        },
        "explanation": {
          "en": "`header` and `footer` default to returning `''` (safe to skip), while `body` throws if not overridden - the base class distinguishes steps every report needs from steps that are merely optional.",
          "ru": "`header` и `footer` по умолчанию возвращают `''` (их можно не переопределять), а `body` бросает исключение, если не переопределён - базовый класс различает шаги, обязательные для каждого отчёта, и шаги, которые можно опустить."
        }
      },
      {
        "lines": [
          19,
          23
        ],
        "title": {
          "en": "HtmlReport overrides all three hooks",
          "ru": "HtmlReport переопределяет все три хука"
        },
        "explanation": {
          "en": "It supplies `header`, `body` and `footer` to wrap the rows in `<h1>`/`<ul>`/`<li>` tags - `generate` itself is never touched or repeated here.",
          "ru": "Он предоставляет `header`, `body` и `footer`, чтобы обернуть строки в теги `<h1>`/`<ul>`/`<li>` - сам `generate` здесь не трогается и не дублируется."
        }
      },
      {
        "lines": [
          25,
          28
        ],
        "title": {
          "en": "MarkdownReport only overrides header and body, inheriting the default footer",
          "ru": "MarkdownReport переопределяет только header и body, наследуя footer по умолчанию"
        },
        "explanation": {
          "en": "It skips overriding `footer`, so `generate` will call the base class's version that returns `''`, which then gets filtered out by `parts.filter(Boolean)`.",
          "ru": "Он не переопределяет `footer`, поэтому `generate` вызывает версию базового класса, возвращающую `''`, которая затем отфильтровывается через `parts.filter(Boolean)`."
        }
      },
      {
        "lines": [
          30,
          33
        ],
        "title": {
          "en": "html.generate() runs the fixed algorithm using HTML-specific steps",
          "ru": "html.generate() выполняет фиксированный алгоритм, используя HTML-специфичные шаги"
        },
        "explanation": {
          "en": "`html.generate('Books', rows)` calls the inherited `generate`, which invokes `HtmlReport`'s `header`, `body` and `footer` in sequence, producing a full `<h1>...</h1><ul>...</ul>` block.",
          "ru": "`html.generate('Books', rows)` вызывает унаследованный `generate`, который последовательно вызывает `header`, `body` и `footer` из `HtmlReport`, формируя полный блок `<h1>...</h1><ul>...</ul>`."
        }
      },
      {
        "lines": [
          35,
          36
        ],
        "title": {
          "en": "md.generate() runs the exact same algorithm, producing Markdown instead",
          "ru": "md.generate() выполняет тот же самый алгоритм, но получая на выходе Markdown"
        },
        "explanation": {
          "en": "`md.generate('Books', rows)` goes through the identical `header → body → footer → filter → join` sequence in `generate`, but since `MarkdownReport` fills the hooks differently, the output is a `# Books` heading with `- ` list items and no closing footer line.",
          "ru": "`md.generate('Books', rows)` проходит ту же самую последовательность `header → body → footer → filter → join` внутри `generate`, но поскольку `MarkdownReport` заполняет хуки иначе, на выходе получается заголовок `# Books` со списком через `- ` и без завершающей строки footer."
        }
      }
    ],
    "python": [
      {
        "lines": [
          6,
          8
        ],
        "title": {
          "en": "generate() is the template method - it fixes the algorithm's skeleton",
          "ru": "generate() - шаблонный метод, он фиксирует скелет алгоритма"
        },
        "explanation": {
          "en": "`generate` always calls `self.header(title)`, `self.body(rows)`, `self.footer()` in that order, filters truthy parts, and joins them - subclasses can't reorder or skip these steps, only fill in what each one produces.",
          "ru": "`generate` всегда вызывает `self.header(title)`, `self.body(rows)`, `self.footer()` именно в этом порядке, отфильтровывает непустые части и склеивает результат - подклассы не могут изменить порядок или пропустить шаги, они могут лишь наполнить каждый шаг содержимым."
        }
      },
      {
        "lines": [
          10,
          14
        ],
        "title": {
          "en": "header/footer are optional hooks, body is a required hook",
          "ru": "header/footer - необязательные хуки, а body - обязательный"
        },
        "explanation": {
          "en": "`header` and `footer` default to returning `''` (safe to skip), while `body` is `@abstractmethod` - the base class distinguishes steps every report needs from steps that are merely optional.",
          "ru": "`header` и `footer` по умолчанию возвращают `''` (их можно не переопределять), а `body` объявлен как `@abstractmethod` - базовый класс различает шаги, обязательные для каждого отчёта, и шаги, которые можно опустить."
        }
      },
      {
        "lines": [
          17,
          25
        ],
        "title": {
          "en": "HtmlReport overrides all three hooks",
          "ru": "HtmlReport переопределяет все три хука"
        },
        "explanation": {
          "en": "It supplies `header`, `body` and `footer` to wrap the rows in `<h1>`/`<ul>`/`<li>` tags - `generate` itself is never touched or repeated here.",
          "ru": "Он предоставляет `header`, `body` и `footer`, чтобы обернуть строки в теги `<h1>`/`<ul>`/`<li>` - сам `generate` здесь не трогается и не дублируется."
        }
      },
      {
        "lines": [
          28,
          33
        ],
        "title": {
          "en": "MarkdownReport only overrides header and body, inheriting the default footer",
          "ru": "MarkdownReport переопределяет только header и body, наследуя footer по умолчанию"
        },
        "explanation": {
          "en": "It skips overriding `footer`, so `generate` will call the base class's version that returns `''`, which then gets filtered out by the `if p` condition in the join.",
          "ru": "Он не переопределяет `footer`, поэтому `generate` вызывает версию базового класса, возвращающую `''`, которая затем отфильтровывается условием `if p` при склейке."
        }
      },
      {
        "lines": [
          38,
          39
        ],
        "title": {
          "en": "html.generate() runs the fixed algorithm using HTML-specific steps",
          "ru": "html.generate() выполняет фиксированный алгоритм, используя HTML-специфичные шаги"
        },
        "explanation": {
          "en": "`html.generate('Books', rows)` calls the inherited `generate`, which invokes `HtmlReport`'s `header`, `body` and `footer` in sequence, producing a full `<h1>...</h1><ul>...</ul>` block.",
          "ru": "`html.generate('Books', rows)` вызывает унаследованный `generate`, который последовательно вызывает `header`, `body` и `footer` из `HtmlReport`, формируя полный блок `<h1>...</h1><ul>...</ul>`."
        }
      },
      {
        "lines": [
          41,
          42
        ],
        "title": {
          "en": "md.generate() runs the exact same algorithm, producing Markdown instead",
          "ru": "md.generate() выполняет тот же самый алгоритм, но получая на выходе Markdown"
        },
        "explanation": {
          "en": "`md.generate('Books', rows)` goes through the identical `header → body → footer → filter → join` sequence in `generate`, but since `MarkdownReport` fills the hooks differently, the output is a `# Books` heading with `- ` list items and no closing footer line.",
          "ru": "`md.generate('Books', rows)` проходит ту же самую последовательность `header → body → footer → filter → join` внутри `generate`, но поскольку `MarkdownReport` заполняет хуки иначе, на выходе получается заголовок `# Books` со списком через `- ` и без завершающей строки footer."
        }
      }
    ]
  },
  "pros": [
    {
      "en": "Subclasses only need to fill in the steps that actually vary, instead of re-implementing the whole algorithm.",
      "ru": "Подклассам нужно реализовать только те шаги, которые действительно отличаются, а не весь алгоритм заново."
    },
    {
      "en": "The parts of the algorithm that never change live in one place, in the base class, instead of being copy-pasted across every subclass.",
      "ru": "Неизменные части алгоритма находятся в одном месте - в базовом классе, а не копируются в каждый подкласс."
    },
    {
      "en": "Because the sequence of steps is fixed by the base class, subclasses cannot accidentally reorder or skip a stage of the algorithm.",
      "ru": "Поскольку порядок шагов задан базовым классом, подклассы не могут случайно поменять их местами или пропустить один из этапов."
    }
  ],
  "cons": [
    {
      "en": "Every new step added to the skeleton means another method every existing subclass may need to review or override.",
      "ru": "Каждый новый шаг, добавленный в каркас, - это ещё один метод, который может потребовать внимания или переопределения во всех существующих подклассах."
    },
    {
      "en": "A subclass that overrides a hook carelessly can break an assumption the base algorithm relies on, since the base class can't enforce what the override actually does.",
      "ru": "Подкласс, небрежно переопределивший хук, может нарушить предположение, на которое опирается базовый алгоритм, - ведь базовый класс не может проконтролировать, что именно делает переопределение."
    },
    {
      "en": "Because the algorithm's shape is fixed through inheritance, adding a genuinely different variant usually means a new subclass rather than a runtime choice - Strategy is a better fit when that flexibility matters.",
      "ru": "Поскольку форма алгоритма закреплена через наследование, для действительно нового варианта обычно нужен новый подкласс, а не выбор во время выполнения - если такая гибкость важна, лучше подойдёт Стратегия."
    }
  ],
  "when_to_use": [
    {
      "en": "Reach for Template Method when subclasses should be free to customize a few specific steps of an algorithm, but must not be able to change its overall sequence.",
      "ru": "Применяйте Шаблонный метод, когда подклассам нужно разрешить настраивать отдельные шаги алгоритма, но нельзя позволять менять его общую последовательность."
    },
    {
      "en": "It also fits when you notice several classes implementing almost the same algorithm with small variations, which today forces you to update every copy whenever the shared logic changes.",
      "ru": "Паттерн также уместен, если несколько классов реализуют почти одинаковый алгоритм с небольшими отличиями, из-за чего сейчас при изменении общей логики приходится обновлять каждую копию."
    }
  ],
  "real_world_examples": [
    {
      "en": "**Data parsers** - a document parser follows the same steps (open, parse, close) for CSV, JSON, and XML files, but each format requires different parsing logic.",
      "ru": "**Парсеры данных** - парсер документов следует одним и тем же шагам (открыть, разобрать, закрыть) для файлов CSV, JSON и XML, но каждый формат требует своей логики разбора."
    },
    {
      "en": "**Web frameworks** - request handling pipelines (authenticate, authorize, handle, respond) define a skeleton that controllers override with specific handler logic.",
      "ru": "**Веб-фреймворки** - конвейеры обработки запросов (аутентификация, авторизация, обработка, ответ) определяют скелет, который контроллеры переопределяют собственной логикой."
    },
    {
      "en": "**Game AI** - a turn-based AI defines a fixed turn structure (collect resources, build, attack) while different difficulty levels override each step with varying strategies.",
      "ru": "**ИИ игры** - пошаговый ИИ определяет фиксированную структуру хода (сбор ресурсов, строительство, атака), а разные уровни сложности переопределяют каждый шаг различными стратегиями."
    }
  ],
  "related_patterns": [
    "strategy",
    "factory-method",
    "decorator",
    "observer"
  ],
  "quiz": [
    {
      "question": {
        "en": "What does Template Method fix in the base class, while leaving to subclasses?",
        "ru": "Что Шаблонный метод закрепляет в базовом классе, оставляя подклассам?"
      },
      "hint": {
        "en": "Separate the order operations happen in from what each operation actually does.",
        "ru": "Отделите порядок выполнения операций от того, что каждая операция делает на самом деле."
      },
      "options": [
        {
          "en": "The overall shape of an algorithm, leaving steps to subclasses",
          "ru": "Общую последовательность алгоритма, оставляя шаги подклассам"
        },
        {
          "en": "The entire algorithm including every step, leaving nothing for subclasses",
          "ru": "Весь алгоритм целиком, включая каждый шаг, не оставляя ничего подклассам"
        },
        {
          "en": "Only error handling, leaving the main logic to subclasses",
          "ru": "Только обработку ошибок, оставляя основную логику подклассам"
        },
        {
          "en": "Nothing - subclasses define both the sequence and the steps",
          "ru": "Ничего - подклассы сами определяют и последовательность, и шаги"
        }
      ],
      "correct": 0,
      "explanation": {
        "en": "Take the report generator: the base class fixes the sequence - title, content rows, footer - while HtmlReport and MarkdownReport each fill in their own markup for those steps. Add a timestamp section later, and it only needs to change in one place.",
        "ru": "Возьмём генератор отчётов: базовый класс закрепляет последовательность - заголовок, строки содержимого, подвал, - а HtmlReport и MarkdownReport заполняют эти шаги собственной разметкой. Понадобится добавить раздел с меткой времени - менять придётся только одно место."
      }
    },
    {
      "question": {
        "en": "In the reporting-tool example, what do the HTML and Markdown formats have in common?",
        "ru": "В примере с инструментом для отчётов что общего у форматов HTML и Markdown?"
      },
      "hint": {
        "en": "The two formats produce very different markup, but what shape do both documents still follow?",
        "ru": "Оба формата дают совершенно разную разметку, но какую структуру всё же сохраняют оба документа?"
      },
      "options": [
        {
          "en": "The same overall structure: title header, content rows, footer",
          "ru": "Одну и ту же общую структуру: заголовок, строки содержимого, колонтитул"
        },
        {
          "en": "The exact same markup syntax for every element",
          "ru": "Совершенно одинаковый синтаксис разметки для каждого элемента"
        },
        {
          "en": "Neither format supports a footer section at all",
          "ru": "Ни один из форматов вообще не поддерживает нижний колонтитул"
        },
        {
          "en": "Both formats are generated by completely unrelated tools",
          "ru": "Оба формата генерируются никак не связанными друг с другом инструментами"
        }
      ],
      "correct": 0,
      "explanation": {
        "en": "Both formats follow the same overall structure - a title header, a list of content rows, and a footer - but the exact markup for each step differs, which is exactly what Template Method is built to share.",
        "ru": "Оба формата следуют одной и той же общей структуре - заголовок с названием, список строк с содержимым и нижний колонтитул, - но конкретная разметка для каждого шага различается, и именно это Шаблонный метод и призван обобщить."
      }
    },
    {
      "question": {
        "en": "What goes wrong if the report-generation algorithm is copied entirely into each format's class (HTML, Markdown)?",
        "ru": "Что идёт не так, если алгоритм генерации отчёта полностью копируется в класс каждого формата (HTML, Markdown)?"
      },
      "hint": {
        "en": "If you needed to insert a new section into every report format, how many places would you have to touch with this design?",
        "ru": "Если бы понадобилось добавить новую секцию во все форматы отчёта, сколько мест пришлось бы менять при таком подходе?"
      },
      "options": [
        {
          "en": "Changing the overall flow requires editing every class",
          "ru": "Изменение общего потока требует правки каждого класса"
        },
        {
          "en": "HTML and Markdown reports become impossible to generate at all",
          "ru": "Генерация отчётов в HTML и Markdown становится вообще невозможной"
        },
        {
          "en": "The title header and footer stop working in every format",
          "ru": "Заголовок и нижний колонтитул перестают работать во всех форматах"
        },
        {
          "en": "It automatically satisfies the Open/Closed Principle",
          "ru": "Это автоматически удовлетворяет принципу открытости/закрытости"
        }
      ],
      "correct": 0,
      "explanation": {
        "en": "Copying the entire algorithm into each report class means changing the overall flow, like adding a timestamp section, requires modifying every class individually.",
        "ru": "Копирование всего алгоритма в каждый класс отчёта означает, что изменение общего потока, например добавление секции с временной меткой, требует изменения каждого класса отдельно."
      }
    },
    {
      "question": {
        "en": "What is the alternative approach of packing all logic into one class with conditionals, and what's wrong with it?",
        "ru": "В чём заключается альтернативный подход - собрать всю логику в одном классе с условными операторами - и что в нём не так?"
      },
      "hint": {
        "en": "Every new format needs a new branch inside that one class - which principle does that put under pressure?",
        "ru": "Для каждого нового формата в этом одном классе нужна новая ветка - какой принцип это нарушает?"
      },
      "options": [
        {
          "en": "It violates the Open/Closed Principle with every new format",
          "ru": "Он нарушает принцип открытости/закрытости при каждом новом формате"
        },
        {
          "en": "It makes HTML and Markdown output identical to each other",
          "ru": "Он делает вывод HTML и Markdown идентичным друг другу"
        },
        {
          "en": "It removes the need for a title header entirely",
          "ru": "Он полностью устраняет необходимость в заголовке"
        },
        {
          "en": "It has no downsides compared to Template Method",
          "ru": "У него нет никаких недостатков по сравнению с Шаблонным методом"
        }
      ],
      "correct": 0,
      "explanation": {
        "en": "Packing all the logic into one class and using conditionals to choose the output format violates the Open/Closed Principle every time a new format is added.",
        "ru": "Помещение всей логики в один класс с использованием условных операторов для выбора формата вывода нарушает принцип открытости/закрытости при добавлении каждого нового формата."
      }
    },
    {
      "question": {
        "en": "What is the difference between an abstract step and a hook in Template Method?",
        "ru": "В чём разница между абстрактным шагом и хуком в Шаблонном методе?"
      },
      "hint": {
        "en": "Is a step mandatory for every subclass to fill in, or does it already work fine without anyone touching it?",
        "ru": "Обязан ли каждый подкласс заполнить этот шаг сам, или он и так прекрасно работает, даже если его никто не трогал?"
      },
      "options": [
        {
          "en": "Abstract steps must be implemented; hooks have a default",
          "ru": "Абстрактные шаги обязательны; хуки имеют реализацию по умолчанию"
        },
        {
          "en": "There is no difference - both terms describe the exact same thing",
          "ru": "Разницы нет - оба термина описывают одно и то же"
        },
        {
          "en": "Hooks must always be overridden, while abstract steps never can be",
          "ru": "Хуки всегда обязаны быть переопределены, а абстрактные шаги - никогда"
        },
        {
          "en": "Abstract steps live outside the template method entirely",
          "ru": "Абстрактные шаги вообще не входят в шаблонный метод"
        }
      ],
      "correct": 0,
      "explanation": {
        "en": "Steps may either be abstract (forcing subclasses to provide an implementation) or have a default implementation that subclasses can optionally override (hooks).",
        "ru": "Шаги могут быть abstract (принуждая подклассы предоставлять реализацию) или иметь реализацию по умолчанию, которую подклассы могут опционально переопределить (хуки)."
      }
    },
    {
      "question": {
        "en": "What does Template Method guarantee about the algorithm's structure?",
        "ru": "Что Шаблонный метод гарантирует в отношении структуры алгоритма?"
      },
      "hint": {
        "en": "Subclasses override individual steps - does that ever require touching the method that calls them in order?",
        "ru": "Подклассы переопределяют отдельные шаги - требует ли это когда-нибудь правки метода, который вызывает их по порядку?"
      },
      "options": [
        {
          "en": "It stays the same while individual steps remain customizable",
          "ru": "Она остаётся неизменной, в то время как отдельные шаги можно свободно настраивать"
        },
        {
          "en": "It changes completely with every new subclass",
          "ru": "Она полностью меняется с каждым новым подклассом"
        },
        {
          "en": "It must be rewritten every time a step is overridden",
          "ru": "Её приходится переписывать каждый раз при переопределении шага"
        },
        {
          "en": "It is decided at runtime by the client, not the base class",
          "ru": "Она определяется клиентом во время выполнения, а не базовым классом"
        }
      ],
      "correct": 0,
      "explanation": {
        "en": "Subclasses can override specific steps without touching the template method itself, which guarantees the overall algorithm structure stays the same while individual steps remain customizable.",
        "ru": "Подклассы могут переопределять конкретные шаги, не трогая шаблонный метод, что гарантирует неизменность общей структуры алгоритма при свободной настройке отдельных шагов."
      }
    },
    {
      "question": {
        "en": "According to the pros listed for Template Method, what do subclasses need to implement?",
        "ru": "Согласно перечисленным преимуществам Шаблонного метода, что нужно реализовать подклассам?"
      },
      "hint": {
        "en": "Compare filling in only what varies to rebuilding the whole algorithm from scratch.",
        "ru": "Сравните заполнение только того, что варьируется, с полной переработкой алгоритма с нуля."
      },
      "options": [
        {
          "en": "Only the steps that actually vary, not the whole algorithm",
          "ru": "Только те шаги, которые действительно отличаются, а не весь алгоритм"
        },
        {
          "en": "Every single step, including the ones that never change",
          "ru": "Каждый отдельный шаг, включая те, что никогда не меняются"
        },
        {
          "en": "A brand-new template method for each subclass",
          "ru": "Совершенно новый шаблонный метод для каждого подкласса"
        },
        {
          "en": "Nothing - the base class provides every step by default",
          "ru": "Ничего - базовый класс предоставляет каждый шаг по умолчанию"
        }
      ],
      "correct": 0,
      "explanation": {
        "en": "Subclasses only need to fill in the steps that actually vary, instead of re-implementing the whole algorithm.",
        "ru": "Подклассам нужно реализовать только те шаги, которые действительно отличаются, а не весь алгоритм заново."
      }
    },
    {
      "question": {
        "en": "What is a ConcreteClass not allowed to do in Template Method?",
        "ru": "Чего не может делать Конкретный класс в Шаблонном методе?"
      },
      "hint": {
        "en": "ConcreteClass fills in the primitive operations - but which one method stays off-limits to it?",
        "ru": "Конкретный класс реализует примитивные операции - но какой один метод остаётся ему недоступен для изменения?"
      },
      "options": [
        {
          "en": "Override the template method itself",
          "ru": "Переопределять сам шаблонный метод"
        },
        {
          "en": "Implement any of the abstract primitive operations",
          "ru": "Реализовывать любую из абстрактных примитивных операций"
        },
        {
          "en": "Provide its own version of a hook method",
          "ru": "Предоставлять собственную версию метода-хука"
        },
        {
          "en": "Be instantiated and used by client code",
          "ru": "Быть созданным и использованным клиентским кодом"
        }
      ],
      "correct": 0,
      "explanation": {
        "en": "ConcreteClass implements the abstract primitive operations to carry out the subclass-specific steps of the algorithm, but does not override the template method itself.",
        "ru": "Конкретный класс реализует абстрактные примитивные операции для выполнения специфичных для подкласса шагов алгоритма, но не переопределяет сам шаблонный метод."
      }
    },
    {
      "question": {
        "en": "What is a stated risk of a subclass overriding a hook carelessly?",
        "ru": "Какой риск указан для подкласса, небрежно переопределившего хук?"
      },
      "hint": {
        "en": "The base algorithm assumes each step behaves a certain way - what can the base class not do about a bad override?",
        "ru": "Базовый алгоритм предполагает, что каждый шаг ведёт себя определённым образом - что базовый класс не может сделать с плохим переопределением?"
      },
      "options": [
        {
          "en": "It can break an assumption the base algorithm relies on",
          "ru": "Он может нарушить предположение, на которое опирается базовый алгоритм"
        },
        {
          "en": "It automatically gets rejected by the compiler in every language",
          "ru": "Он автоматически отклоняется компилятором в любом языке"
        },
        {
          "en": "It permanently disables the template method for all subclasses",
          "ru": "Он навсегда отключает шаблонный метод для всех подклассов"
        },
        {
          "en": "It has no real effect since hooks are always optional",
          "ru": "Он не имеет реального эффекта, поскольку хуки всегда необязательны"
        }
      ],
      "correct": 0,
      "explanation": {
        "en": "A subclass that overrides a hook carelessly can break an assumption the base algorithm relies on, since the base class can't enforce what the override actually does.",
        "ru": "Подкласс, небрежно переопределивший хук, может нарушить предположение, на которое опирается базовый алгоритм, - ведь базовый класс не может проконтролировать, что именно делает переопределение."
      }
    },
    {
      "question": {
        "en": "When is Strategy said to be a better fit than Template Method?",
        "ru": "Когда, согласно паттерну, Стратегия подходит лучше, чем Шаблонный метод?"
      },
      "hint": {
        "en": "Template Method locks in the algorithm's shape through inheritance at compile time - what if you need to swap behavior after the program is already running?",
        "ru": "Шаблонный метод фиксирует форму алгоритма через наследование на этапе компиляции - а что если поведение нужно менять уже во время выполнения программы?"
      },
      "options": [
        {
          "en": "When a different variant must be chosen at runtime",
          "ru": "Когда вариант нужно выбирать во время выполнения"
        },
        {
          "en": "Never - Template Method is always strictly superior",
          "ru": "Никогда - Шаблонный метод всегда строго превосходит Стратегию"
        },
        {
          "en": "Only when there is exactly one report format",
          "ru": "Только когда существует ровно один формат отчёта"
        },
        {
          "en": "Only in languages without inheritance support",
          "ru": "Только в языках без поддержки наследования"
        }
      ],
      "correct": 0,
      "explanation": {
        "en": "Because the algorithm's shape is fixed through inheritance, adding a genuinely different variant usually means a new subclass rather than a runtime choice - Strategy is a better fit when that flexibility matters.",
        "ru": "Поскольку форма алгоритма закреплена через наследование, для действительно нового варианта обычно нужен новый подкласс, а не выбор во время выполнения - если такая гибкость важна, лучше подойдёт Стратегия."
      }
    }
  ],
  "visuals": [
    {
      "id": "template-method-skeleton",
      "type": "flow",
      "style": "skeleton",
      "category": "behavioral",
      "width": 900,
      "height": 420,
      "title": {
        "en": "How Template Method works",
        "ru": "Как работает Шаблонный метод"
      },
      "description": {
        "en": "ReportGenerator's generate() is the template method - it always calls header(), body(), and footer() in that exact order. header() and footer() are hooks with an empty default; body() is the one step every subclass must fill in. HtmlReport and MarkdownReport each plug their own step bodies into the same skeleton - the call order itself never changes.",
        "ru": "generate() класса ReportGenerator - это шаблонный метод: он всегда вызывает header(), body() и footer() в строго заданном порядке. header() и footer() - хуки с пустой реализацией по умолчанию; body() - единственный шаг, который обязан реализовать каждый подкласс. HtmlReport и MarkdownReport подставляют в один и тот же скелет свои шаги, но сам порядок вызовов не меняется никогда."
      },
      "caption": {
        "en": "Same order every call. Only the plugged-in steps change.",
        "ru": "Порядок вызовов всегда один. Меняются только подставленные шаги."
      },
      "nodes": [
        {
          "id": "client",
          "role": "client",
          "kind": "actor",
          "icon": "user",
          "label": {
            "en": "Client",
            "ru": "Клиент"
          },
          "subtitle": {
            "en": "runs the report",
            "ru": "запускает отчёт"
          }
        },
        {
          "id": "abstract",
          "role": "skeleton",
          "kind": "abstract",
          "icon": "layers",
          "label": {
            "en": "ReportGenerator",
            "ru": "ReportGenerator"
          },
          "subtitle": {
            "en": "generate()",
            "ru": "generate()"
          },
          "steps": [
            {
              "id": "header",
              "label": "header()"
            },
            {
              "id": "body",
              "label": "body()",
              "variant": "abstract"
            },
            {
              "id": "footer",
              "label": "footer()"
            }
          ]
        },
        {
          "id": "concreteA",
          "role": "concrete",
          "kind": "class",
          "icon": "cube",
          "label": {
            "en": "HtmlReport",
            "ru": "HtmlReport"
          },
          "subtitle": {
            "en": "fills the steps",
            "ru": "заполняет шаги"
          }
        },
        {
          "id": "concreteB",
          "role": "concrete",
          "kind": "class",
          "icon": "cube",
          "label": {
            "en": "MarkdownReport",
            "ru": "MarkdownReport"
          },
          "subtitle": {
            "en": "fills the steps",
            "ru": "заполняет шаги"
          }
        }
      ],
      "edges": [
        {
          "source": "client",
          "target": "abstract",
          "relationship": "flow",
          "label": {
            "en": "generate()",
            "ru": "generate()"
          }
        },
        {
          "source": "abstract",
          "target": "concreteA",
          "relationship": "flow",
          "label": {
            "en": "plugs in",
            "ru": "подставляется"
          }
        },
        {
          "source": "abstract",
          "target": "concreteB",
          "relationship": "flow",
          "label": {
            "en": "plugs in",
            "ru": "подставляется"
          }
        }
      ]
    }
  ]
}
