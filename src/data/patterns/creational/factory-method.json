{
  "slug": "factory-method",
  "name": "Factory Method",
  "category": "creational",
  "complexity": 1,
  "popularity": 3,
  "also_known_as": [
    "Virtual Constructor"
  ],
  "tags": [
    "creational",
    "factory",
    "virtual-constructor"
  ],
  "intent": {
    "en": "Factory Method delegates object creation to a method that subclasses can override, so a base class can work with products whose exact type it doesn't need to know.",
    "ru": "Фабричный метод выносит создание объекта в отдельный метод, который могут переопределять подклассы, — благодаря этому базовому классу не нужно знать точный тип создаваемых продуктов."
  },
  "problem": {
    "en": "A logistics module starts out delivering only by road, so `Logistics.planDelivery()` builds a `Truck` object right inside its own method body. Months later, sea shipping is added. Every place that constructed a `Truck` directly now needs an `if/else` or `switch` on delivery mode, and that branching logic tends to get copy-pasted into every method that needs a transport rather than living in one place.\n\nThe underlying issue is that the class doing the useful work — planning the delivery, tracking it, logging it — is welded to one specific concrete class it happens to instantiate, so extending it to a new mode of transport means editing code that has nothing to do with that new mode conceptually.",
    "ru": "Модуль логистики начинался с доставки только по земле, поэтому `Logistics.planDelivery()` создаёт объект `Truck` прямо внутри своего метода. Спустя месяцы добавляется морская доставка. В каждом месте, где раньше напрямую создавался `Truck`, теперь нужен `if/else` или `switch` по способу доставки, и такую ветвящуюся логику приходится копировать в каждый метод, которому нужен транспорт, вместо того чтобы держать её в одном месте.\n\nКорень проблемы в том, что класс, выполняющий полезную работу — планирование доставки, её отслеживание, логирование, — намертво привязан к одному конкретному классу, который он создаёт, поэтому расширение под новый вид транспорта требует правки кода, концептуально не имеющего отношения к этому новому виду транспорта."
  },
  "solution": {
    "en": "Pull the `new Truck(...)` call out of the method body and into its own method — the **factory method** — and have the rest of the class call that method instead of a constructor directly. The base class only knows that the factory method returns something implementing a common `Transport` interface; it never names a concrete class.\n\nEach delivery mode then becomes a subclass that overrides just the factory method: `RoadLogistics` returns a `Truck`, `SeaLogistics` returns a `Ship`. All the shared logic — planning, tracking, logging — stays untouched in the base class and automatically works with whatever concrete product the subclass decides to hand back.",
    "ru": "Вызов `new Truck(...)` выносится из тела метода в собственный метод — **фабричный метод**, — и остальной код класса вызывает уже этот метод, а не конструктор напрямую. Базовый класс знает лишь, что фабричный метод возвращает нечто, реализующее общий интерфейс `Transport`; он никогда не называет конкретный класс.\n\nКаждый способ доставки становится подклассом, переопределяющим только фабричный метод: `RoadLogistics` возвращает `Truck`, `SeaLogistics` — `Ship`. Вся общая логика — планирование, отслеживание, логирование — остаётся нетронутой в базовом классе и автоматически работает с тем конкретным продуктом, который вернёт подкласс."
  },
  "structure": {
    "description": {
      "en": "The Creator hosts the shared workflow and calls its own factory method whenever it needs a new product, without knowing which concrete class that method will return. Each ConcreteCreator overrides just the factory method to supply one specific ConcreteProduct, and every product — regardless of which creator built it — is used through the same Product interface.",
      "ru": "Создатель содержит общий рабочий процесс и вызывает собственный фабричный метод всякий раз, когда ему нужен новый продукт, не зная, какой конкретный класс этот метод вернёт. Каждый ConcreteCreator переопределяет только фабричный метод, поставляя один конкретный ConcreteProduct, а любой продукт — независимо от того, каким создателем он построен — используется через один и тот же интерфейс Product."
    },
    "participants": [
      {
        "name": "Creator",
        "role": {
          "en": "Contains the workflow that needs a product and calls its own factory method to obtain one, optionally with a default implementation.",
          "ru": "Содержит рабочий процесс, которому нужен продукт, и вызывает собственный фабричный метод, чтобы его получить, при необходимости с реализацией по умолчанию."
        }
      },
      {
        "name": "ConcreteCreator",
        "role": {
          "en": "Overrides only the factory method, returning one specific ConcreteProduct instance while inheriting all the shared workflow unchanged.",
          "ru": "Переопределяет только фабричный метод, возвращая экземпляр одного конкретного ConcreteProduct, при этом наследуя весь общий рабочий процесс без изменений."
        }
      },
      {
        "name": "Product",
        "role": {
          "en": "The interface every product must satisfy, letting the Creator's workflow stay identical regardless of which concrete product is actually in play.",
          "ru": "Интерфейс, которому должен соответствовать любой продукт, — благодаря ему рабочий процесс Создателя остаётся неизменным независимо от того, какой конкретный продукт используется."
        }
      },
      {
        "name": "ConcreteProduct",
        "role": {
          "en": "A specific class implementing Product, instantiated only inside the matching ConcreteCreator's factory method.",
          "ru": "Конкретный класс, реализующий Product, создаваемый только внутри фабричного метода соответствующего ConcreteCreator."
        }
      }
    ]
  },
  "implementation": {
    "javascript": "class Transport {\n  deliver() {\n    throw new Error('deliver() must be implemented');\n  }\n}\n\nclass Truck extends Transport {\n  deliver() {\n    return 'Deliver by land in a box';\n  }\n}\n\nclass Ship extends Transport {\n  deliver() {\n    return 'Deliver by sea in a container';\n  }\n}\n\nclass Logistics {\n  createTransport() {\n    throw new Error('createTransport() must be implemented');\n  }\n\n  planDelivery() {\n    const transport = this.createTransport();\n    return `Planning: ${transport.deliver()}`;\n  }\n}\n\nclass RoadLogistics extends Logistics {\n  createTransport() {\n    return new Truck();\n  }\n}\n\nclass SeaLogistics extends Logistics {\n  createTransport() {\n    return new Ship();\n  }\n}\n\nconst road = new RoadLogistics();\nconsole.log(road.planDelivery());\n\nconst sea = new SeaLogistics();\nconsole.log(sea.planDelivery());",
    "typescript": "interface Transport {\n  deliver(): string;\n}\n\nclass Truck implements Transport {\n  deliver(): string {\n    return 'Deliver by land in a box';\n  }\n}\n\nclass Ship implements Transport {\n  deliver(): string {\n    return 'Deliver by sea in a container';\n  }\n}\n\nabstract class Logistics {\n  abstract createTransport(): Transport;\n\n  planDelivery(): string {\n    const transport = this.createTransport();\n    return `Planning: ${transport.deliver()}`;\n  }\n}\n\nclass RoadLogistics extends Logistics {\n  createTransport(): Transport {\n    return new Truck();\n  }\n}\n\nclass SeaLogistics extends Logistics {\n  createTransport(): Transport {\n    return new Ship();\n  }\n}\n\nconst road = new RoadLogistics();\nconsole.log(road.planDelivery());\n\nconst sea = new SeaLogistics();\nconsole.log(sea.planDelivery());",
    "java": "interface Transport { String deliver(); }\n\nclass Truck implements Transport { public String deliver() { return \"Deliver by land in a box\"; } }\nclass Ship  implements Transport { public String deliver() { return \"Deliver by sea in a container\"; } }\n\nabstract class Logistics {\n    public abstract Transport createTransport();\n\n    public String planDelivery() {\n        return \"Planning: \" + createTransport().deliver();\n    }\n}\n\nclass RoadLogistics extends Logistics {\n    public Transport createTransport() { return new Truck(); }\n}\n\nclass SeaLogistics extends Logistics {\n    public Transport createTransport() { return new Ship(); }\n}\n\nLogistics road = new RoadLogistics();\nSystem.out.println(road.planDelivery());\n\nLogistics sea = new SeaLogistics();\nSystem.out.println(sea.planDelivery());",
    "csharp": "interface ITransport { string Deliver(); }\n\nclass Truck : ITransport { public string Deliver() => \"Deliver by land in a box\"; }\nclass Ship  : ITransport { public string Deliver() => \"Deliver by sea in a container\"; }\n\nabstract class Logistics\n{\n    public abstract ITransport CreateTransport();\n\n    public string PlanDelivery() => $\"Planning: {CreateTransport().Deliver()}\";\n}\n\nclass RoadLogistics : Logistics { public override ITransport CreateTransport() => new Truck(); }\nclass SeaLogistics  : Logistics { public override ITransport CreateTransport() => new Ship(); }\n\nLogistics road = new RoadLogistics();\nConsole.WriteLine(road.PlanDelivery());\n\nLogistics sea = new SeaLogistics();\nConsole.WriteLine(sea.PlanDelivery());",
    "python": "from abc import ABC, abstractmethod\n\nclass Transport(ABC):\n    @abstractmethod\n    def deliver(self) -> str:\n        pass\n\nclass Truck(Transport):\n    def deliver(self) -> str:\n        return 'Deliver by land in a box'\n\nclass Ship(Transport):\n    def deliver(self) -> str:\n        return 'Deliver by sea in a container'\n\nclass Logistics(ABC):\n    @abstractmethod\n    def create_transport(self) -> Transport:\n        pass\n\n    def plan_delivery(self) -> str:\n        transport = self.create_transport()\n        return f'Planning: {transport.deliver()}'\n\nclass RoadLogistics(Logistics):\n    def create_transport(self) -> Transport:\n        return Truck()\n\nclass SeaLogistics(Logistics):\n    def create_transport(self) -> Transport:\n        return Ship()\n\nroad = RoadLogistics()\nprint(road.plan_delivery())\n\nsea = SeaLogistics()\nprint(sea.plan_delivery())"
  },
  "pros": [
    {
      "en": "The base class's workflow never names a concrete product class, so it stays valid no matter how many product types get added later.",
      "ru": "Рабочий процесс базового класса ни разу не называет конкретный класс продукта, поэтому он остаётся рабочим независимо от того, сколько типов продуктов добавят позже."
    },
    {
      "en": "All the object-creation decisions for a family of related classes live in one overridable method instead of being copy-pasted wherever a new instance is needed.",
      "ru": "Все решения о создании объектов для семейства связанных классов сосредоточены в одном переопределяемом методе, а не разбросаны копипастой по местам, где нужен новый экземпляр."
    },
    {
      "en": "Adding a new product variant is purely additive — write one new ConcreteCreator/ConcreteProduct pair, and nothing in the existing hierarchy has to change.",
      "ru": "Добавление нового варианта продукта — чисто аддитивная операция: пишется одна новая пара ConcreteCreator/ConcreteProduct, а существующая иерархия не меняется."
    }
  ],
  "cons": [
    {
      "en": "Each new product variant typically means a new subclass, so a family with many variants can produce a sizeable parallel class hierarchy.",
      "ru": "Каждый новый вариант продукта обычно означает новый подкласс, поэтому семейство с большим числом вариантов может породить заметную параллельную иерархию классов."
    },
    {
      "en": "For a class that only ever needs to build one type of object, introducing a factory method is pure overhead with no payoff.",
      "ru": "Для класса, которому нужно создавать объекты только одного типа, введение фабричного метода — чистые накладные расходы без какой-либо выгоды."
    }
  ],
  "when_to_use": [
    {
      "en": "Reach for it when a class can't know in advance the exact type of object it will need to create — that decision belongs to a subclass or is resolved at runtime.",
      "ru": "Применяйте паттерн, когда класс заранее не может знать точный тип объекта, который ему нужно создать, — это решение принадлежит подклассу или разрешается во время выполнения."
    },
    {
      "en": "Reach for it when you're building a library or framework and want callers to be able to plug in their own product types without modifying your internal code.",
      "ru": "Применяйте паттерн, когда вы создаёте библиотеку или фреймворк и хотите, чтобы пользователи могли подключать собственные типы продуктов, не изменяя ваш внутренний код."
    },
    {
      "en": "Reach for it when several classes share almost identical logic and differ only in which kind of object they instantiate at one specific point.",
      "ru": "Применяйте паттерн, когда несколько классов имеют почти идентичную логику и различаются только тем, объект какого типа они создают в одной конкретной точке."
    }
  ],
  "real_world_examples": [
    {
      "en": "**`document.createElement(tag)`** — the browser returns different concrete element classes (`HTMLButtonElement`, `HTMLInputElement`) behind the same `Element` interface, chosen by the tag argument.",
      "ru": "**`document.createElement(tag)`** — браузер возвращает разные конкретные классы элементов (`HTMLButtonElement`, `HTMLInputElement`) за одним интерфейсом `Element`, выбирая их по имени тега."
    },
    {
      "en": "**Java's `Calendar.getInstance()`** — returns a locale-appropriate concrete calendar subclass (Gregorian, Buddhist, Japanese) without the caller ever choosing the class directly.",
      "ru": "**`Calendar.getInstance()` в Java** — возвращает конкретный подкласс календаря, подходящий для локали (григорианский, буддийский, японский), причём вызывающий код никогда не выбирает класс напрямую."
    },
    {
      "en": "**Logger factories in frameworks like Log4j/SLF4J** — `LoggerFactory.getLogger()` hands back a concrete logger implementation appropriate for the configured backend, behind one common `Logger` interface.",
      "ru": "**Фабрики логгеров во фреймворках вроде Log4j/SLF4J** — `LoggerFactory.getLogger()` возвращает конкретную реализацию логгера, подходящую под настроенный бэкенд, за одним общим интерфейсом `Logger`."
    }
  ],
  "related_patterns": [
    "abstract-factory",
    "template-method",
    "prototype",
    "iterator"
  ],
  "quiz": [
    {
      "question": {
        "en": "What does Factory Method delegate to a subclass-overridable method?",
        "ru": "Что Фабричный метод передаёт методу, переопределяемому подклассами?"
      },
      "options": [
        {
          "en": "Object creation, so the base class can work with products without knowing their exact type",
          "ru": "Создание объекта, чтобы базовый класс мог работать с продуктами, не зная их точного типа"
        },
        {
          "en": "The entire business logic of the class",
          "ru": "Всю бизнес-логику класса"
        },
        {
          "en": "Deep copying of an existing instance",
          "ru": "Глубокое копирование существующего экземпляра"
        },
        {
          "en": "Access control to a shared single instance",
          "ru": "Контроль доступа к единому общему экземпляру"
        }
      ],
      "correct": 0,
      "explanation": {
        "en": "The base class only knows the factory method returns something implementing a common Transport interface — it never names a concrete class.",
        "ru": "Базовый класс знает лишь, что фабричный метод возвращает нечто, реализующее общий интерфейс Transport, — он никогда не называет конкретный класс."
      }
    },
    {
      "question": {
        "en": "What goes wrong when Logistics.planDelivery() constructs a Truck directly and sea shipping is later added?",
        "ru": "Что идёт не так, если Logistics.planDelivery() создаёт Truck напрямую, а затем добавляется морская доставка?"
      },
      "options": [
        {
          "en": "Every place that constructed a Truck directly now needs branching logic on delivery mode, often copy-pasted across methods",
          "ru": "В каждом месте, где раньше напрямую создавался Truck, теперь нужна ветвящаяся логика по способу доставки, часто копируемая по методам"
        },
        {
          "en": "The Truck class stops working entirely",
          "ru": "Класс Truck полностью перестаёт работать"
        },
        {
          "en": "The Transport interface must be deleted",
          "ru": "Интерфейс Transport приходится удалить"
        },
        {
          "en": "Ship objects can no longer be created at all",
          "ru": "Объекты Ship вообще нельзя больше создавать"
        }
      ],
      "correct": 0,
      "explanation": {
        "en": "The class doing the useful work is welded to one specific concrete class, so extending it to a new transport mode means editing code that conceptually has nothing to do with that mode.",
        "ru": "Класс, выполняющий полезную работу, намертво привязан к одному конкретному классу, поэтому расширение под новый вид транспорта требует правки кода, концептуально не связанного с этим видом транспорта."
      }
    },
    {
      "question": {
        "en": "In the solution, how does RoadLogistics differ from SeaLogistics?",
        "ru": "В решении — чем RoadLogistics отличается от SeaLogistics?"
      },
      "options": [
        {
          "en": "They override only the factory method: RoadLogistics returns a Truck, SeaLogistics returns a Ship, while shared logic stays in the base class",
          "ru": "Они переопределяют только фабричный метод: RoadLogistics возвращает Truck, SeaLogistics — Ship, а общая логика остаётся в базовом классе"
        },
        {
          "en": "They each reimplement planDelivery() from scratch with duplicated logic",
          "ru": "Каждый заново реализует planDelivery() с дублированием логики"
        },
        {
          "en": "SeaLogistics does not extend the Logistics base class",
          "ru": "SeaLogistics не наследуется от базового класса Logistics"
        },
        {
          "en": "RoadLogistics has no createTransport() method",
          "ru": "У RoadLogistics нет метода createTransport()"
        }
      ],
      "correct": 0,
      "explanation": {
        "en": "Each delivery mode becomes a subclass overriding just the factory method; all shared logic (planning, tracking, logging) stays untouched in the base class.",
        "ru": "Каждый способ доставки становится подклассом, переопределяющим только фабричный метод; вся общая логика (планирование, отслеживание, логирование) остаётся нетронутой в базовом классе."
      }
    },
    {
      "question": {
        "en": "What is a stated benefit of adding a new product variant with Factory Method?",
        "ru": "Какое преимущество Фабричного метода указано при добавлении нового варианта продукта?"
      },
      "options": [
        {
          "en": "It's purely additive — one new ConcreteCreator/ConcreteProduct pair, and nothing in the existing hierarchy has to change",
          "ru": "Это чисто аддитивная операция — одна новая пара ConcreteCreator/ConcreteProduct, а существующая иерархия не меняется"
        },
        {
          "en": "It requires rewriting the base class entirely",
          "ru": "Требует полной переписи базового класса"
        },
        {
          "en": "It forces every existing subclass to be renamed",
          "ru": "Заставляет переименовывать все существующие подклассы"
        },
        {
          "en": "It only works if there is exactly one product type",
          "ru": "Работает только при наличии ровно одного типа продукта"
        }
      ],
      "correct": 0,
      "explanation": {
        "en": "Adding a new product variant is purely additive — write one new ConcreteCreator/ConcreteProduct pair, and nothing in the existing hierarchy has to change.",
        "ru": "Добавление нового варианта продукта — чисто аддитивная операция: пишется одна новая пара ConcreteCreator/ConcreteProduct, а существующая иерархия не меняется."
      }
    },
    {
      "question": {
        "en": "What is a stated drawback of Factory Method?",
        "ru": "Какой недостаток Фабричного метода указан явно?"
      },
      "options": [
        {
          "en": "Each new product variant typically means a new subclass, so a family with many variants can produce a sizeable parallel class hierarchy",
          "ru": "Каждый новый вариант продукта обычно означает новый подкласс, поэтому семейство с большим числом вариантов может породить заметную параллельную иерархию классов"
        },
        {
          "en": "It makes it impossible to add new transport types",
          "ru": "Он делает невозможным добавление новых типов транспорта"
        },
        {
          "en": "It requires a global synchronization lock",
          "ru": "Он требует глобальной блокировки синхронизации"
        },
        {
          "en": "It prevents the base class from having any shared logic",
          "ru": "Он не позволяет базовому классу содержать общую логику"
        }
      ],
      "correct": 0,
      "explanation": {
        "en": "For a class that only ever needs to build one type of object, or a family with many variants, Factory Method's subclass-per-variant approach can become overhead or a sizeable parallel hierarchy.",
        "ru": "Для класса, которому нужен только один тип объекта, либо для семейства с большим числом вариантов, подход «подкласс на вариант» Фабричного метода превращается в накладные расходы или заметную параллельную иерархию."
      }
    }
  ],
  "visuals": [
    {
      "id": "factory-method-override",
      "type": "flow",
      "style": "override",
      "category": "creational",
      "width": 880,
      "height": 360,
      "title": {
        "en": "How Factory Method works",
        "ru": "Как работает Фабричный метод"
      },
      "description": {
        "en": "The Client only ever calls the Creator's abstract factory method — it never names a concrete class. RoadLogistics is the ConcreteCreator currently plugged into that method, so calling it returns a Truck. The dashed cards behind Creator are the other ConcreteCreators the family could have plugged in instead, each wired to return a different Product — the Client's code would stay exactly the same either way.",
        "ru": "Клиент лишь вызывает абстрактный фабричный метод Создателя — он никогда не называет конкретный класс. RoadLogistics — это ConcreteCreator, который сейчас подключён к этому методу, поэтому вызов возвращает Truck. Пунктирные карточки позади Создателя — другие ConcreteCreator из того же семейства, которые могли бы быть подключены вместо него, каждый со своим Product. Код клиента при этом не изменился бы ни на строчку."
      },
      "caption": {
        "en": "Whichever ConcreteCreator is plugged in decides the Product — the Client never picks the class.",
        "ru": "Какой ConcreteCreator подключён — тот и решает, каким будет Продукт. Клиент класс не выбирает."
      },
      "nodes": [
        {
          "id": "client",
          "role": "client",
          "kind": "actor",
          "icon": "user",
          "label": {
            "en": "Client",
            "ru": "Клиент"
          },
          "subtitle": {
            "en": "planDelivery()",
            "ru": "planDelivery()"
          }
        },
        {
          "id": "creator",
          "role": "creator",
          "kind": "class",
          "icon": "layers",
          "label": {
            "en": "Creator",
            "ru": "Creator"
          },
          "subtitle": {
            "en": "factoryMethod()",
            "ru": "factoryMethod()"
          },
          "concrete": {
            "role": {
              "en": "ConcreteCreator",
              "ru": "ConcreteCreator"
            },
            "label": {
              "en": "RoadLogistics",
              "ru": "RoadLogistics"
            }
          },
          "siblings": 2
        },
        {
          "id": "product",
          "role": "product",
          "kind": "class",
          "icon": "cube",
          "label": {
            "en": "Product",
            "ru": "Product"
          },
          "subtitle": {
            "en": "Truck",
            "ru": "Truck"
          }
        }
      ],
      "edges": [
        {
          "source": "client",
          "target": "creator",
          "relationship": "flow",
          "label": {
            "en": "① planDelivery()",
            "ru": "① planDelivery()"
          }
        },
        {
          "source": "creator",
          "target": "product",
          "relationship": "flow",
          "variant": "create",
          "label": {
            "en": "② factoryMethod()",
            "ru": "② factoryMethod()"
          }
        }
      ]
    }
  ]
}
